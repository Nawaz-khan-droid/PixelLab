# PixelLab — Render.com Free Tier (512MB RAM)
# No matplotlib (Chart.js handles histograms client-side)
# opencv-python-headless avoids libGL.so.1 crash on Render

flask==3.1.1
gunicorn==23.0.0
opencv-python-headless==4.11.0.86
numpy==2.2.6
pillow==11.2.1
requests==2.32.3
python-dotenv==1.1.0
